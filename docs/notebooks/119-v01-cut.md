# Notebook — card 119: cut v0.1, re-cut post 0000

Branch: `119-v01-cut`. Card text: "Replace the draft's aspirational
parts with reality: a real chronicle excerpt from a real seed, real
install steps, honest line counts and timings. Pin tag post/0000 and
publish. The clock is wound."

Worked by Claude on Mike's instruction ("let's tackle card 119 and
see what comes out of that sort of recutting based upon what we've
learned"), remotely, with review to follow.

## The version cut

Smaller than it sounds, by design — `main.lua` had been carrying the
comment "versions get real at v0.1 (card 119)" since card 112:

- `ENGINE_VERSION` `"dev-1"` → `"0.1.0"` (src/main.lua).
- `sonder-dev-1.rockspec` → `sonder-0.1.0-1.rockspec` (git mv +
  version field; LuaRocks wants version-revision).
- `tools/setup.sh`'s fresh-resolve path points at the new rockspec.
- **Left alone on purpose:** `tests/archive_spec.lua`'s "dev-1" /
  "deadbeef" fixtures — they're arbitrary caller-supplied provenance
  values proving the archive stores what it's told, not the engine
  constant. Changing them would imply a coupling that doesn't exist.
- **Golden masters unmoved, as predicted:** engine version lives in
  provenance only, never in event bytes, so the seal can't see it.
  110/110 specs green with zero re-cuts. (The re-cut ledger gains no
  entry; that's the system working.)
- Tags at merge: `post/0000` per the card, and `v0.1.0` for the
  release itself.

## Verification that doubles as content

The worktree had no toolchain (gitignored), which made it a genuine
fresh machine. `./tools/setup.sh` went from nothing to doctor-green in
one command — and that run *is* the verification of the install steps
the re-cut post publishes. The numbers gathered for the post, all
measured on this branch, none estimated:

- 1,970 lines of Lua across `src/` (draft claimed ~800).
- 1,000 ticks of seed 1893: **0.12 s** without the archive, ~0.8 s
  with it (draft claimed "two seconds").
- 3,142 events, 20 wars (first day 86, last day 971), fingerprint
  `e421dc37951300c9`, seal `948cc6022ed8a8c9`.
- The front-door excerpt is the real feed: genesis endowments, the
  ticks 84–96 hunger war, the tick-100 trade at 84¢.
- `--why 327` produces the declaration→hunger→empty-granary ladder
  (event 327 = the day-86 `war.declared`).

## Re-cut decisions (what "based on what we've learned" meant)

- **The manifesto's shape survives; its tenses change.** The four
  bets stay, each now carrying its receipts (posts 0001–0007). The
  147-era additions (the legibility aside, the seed→log→projections
  diagram) stay.
- **The draft's false claims are corrected, not laundered.** New
  "What we got wrong" section — the re-cut's biggest structural
  change, and the part card 147's experiment proved is worth the
  space at every reading level. Four entries: the wrong prophecy
  (`pairs()` never bit us; post 0001 already corrected this and the
  re-cut aligns), the imagined universe vs the real one (heliox/
  Combine/war-moot → grain/hunger/day-86; the names Vessari and
  Khedrun walked from draft to shipped world unchanged), reality
  bigger and faster than the dream (2.5× the code, 16× the speed),
  and the fictional install steps replaced by verified ones.
- **The two-register sentence is now in the manifesto** ("Why we're
  doing it") — the front door should say the simple track exists.
- **Draft banners removed from both tracks**; the old draft stays
  readable in git history, which the post says explicitly.
- Repo URL in the run-it block fixed to the real remote
  (`walesmd/sonder-sim`; the draft said `sonder-sim/sonder`).

## Docs sweep

- README: status header flips Pre-0.1 → v0.1; "still ahead: the v0.1
  cut" replaced; "once v0.1 exists" phrasing retired.
- CLAUDE.md: status flips to "v0.1 is cut", card 148's entry moved
  from "in flight on this branch" (stale on main since PR #12 — my
  phrasing error, noted below) into the done list, card 119 added,
  next-up now cards 122 and 133–146 (130 closed at Mike's call).
- Posts index: 0000 loses its draft mark (tag `post/0000` at merge);
  0009's stale draft mark from the 148 ship removed too.
- Glossary: no new terms; card 118 had already retired "toy
  universe" for "the toy world".

## What we got wrong (candidates for future posts, not this one)

- **"In flight on this branch" merges to main and lies.** Card 148's
  CLAUDE.md status entry said "in flight" and shipped that way in PR
  #12. Written-as-done is the rule now: status text merges with the
  PR that makes it true.
- **The 148 ship left the posts index stale** (0009 marked draft
  after it had merged and been tagged). Caught and fixed here; the
  docs sweep checklist should probably say "re-check the index rows
  of the post being shipped."

## Owed / open

- Mike's review of the re-cut — the "What we got wrong" scorecard
  makes claims about the draft's intent that only Mike can ratify.
- At merge: tags `post/0000` and `v0.1.0`; close card 119.
- Post 0000 has no separate new post number — the re-cut *is* card
  119's post, published at `post/0000`, per the card.
